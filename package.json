{
  "name": "eblett",
  "version": "1.0.0",
  "description": "3D and WebGL experiment",
  "main": "index.js",
  "author": "naffiq",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "three": "^0.107.0"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "parcel": "^1.12.3"
  },
  "scripts": {
    "build": "rm -rf dist && parcel build index.html --public-url /eblett/",
    "deploy": "yarn build && gh-pages -d dist"
  }
}
